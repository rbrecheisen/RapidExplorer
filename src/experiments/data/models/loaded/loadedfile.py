from abc import ABC, abstractmethod


class LoadedFile(ABC):
    # Moet dit een specifiek DICOM type zijn? Of misschien een abstracte class
    # met specifieke implementaties voor DICOM, PNG, etc.?
    pass
